# Build artifacts (generated by `make`; removed by `make clean`)
build/
**/*.o
**/*.a
**/*.so
**/*.dylib

# Internal design docs and rulebook — not part of the published SDK
docs/
CLAUDE.md

# Claude Code harness working dir (agent worktrees, local settings) — never committed
.claude/

# OS / editor cruft
.DS_Store
*.swp

# stray local archive
src.zip
*.zip
